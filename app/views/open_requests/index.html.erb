<%= turbo_stream_from [ "open_requests", current_user.city ] %>

<div class="max-w-2xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-semibold text-stone-900 mb-6">Open walk requests</h1>

  <%= render "open_requests/list", walks: @walks, city: current_user.city %>
</div>
